<UserControl x:Class="Demolyzer.View.CompareParticipantsViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="260" 
             d:DesignWidth="450"
>
  <Border BorderThickness="1" BorderBrush="Gray">
    <Grid>
      <StackPanel VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,10">
          <TextBlock Text="Total Frags:" Foreground="Yellow" VerticalAlignment="Center"/>
          <TextBox Width="50" Margin="5,0,0,0" Text="{Binding TotalFragCount, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
          <StackPanel>
            <ListView ItemsSource="{Binding TeamA}" Height="120">
              <ListView.View>
                <GridView AllowsColumnReorder="False">
                  <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="Name" Width="100"/>
                  <GridViewColumn DisplayMemberBinding="{Binding Path=ExpectedScore}" Header="Expected" Width="60"/>
                </GridView>
              </ListView.View>
            </ListView>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
              <TextBlock Text="{Binding TotalA, StringFormat=Total: {0}}" Foreground="Yellow" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>
          </StackPanel>
          <StackPanel>
            <ListView ItemsSource="{Binding TeamB}" Height="120" Margin="20,0,0,0">
              <ListView.View>
                <GridView AllowsColumnReorder="False">
                  <GridViewColumn DisplayMemberBinding="{Binding Path=Name}" Header="Name" Width="100"/>
                  <GridViewColumn DisplayMemberBinding="{Binding Path=ExpectedScore}" Header="Expected" Width="60"/>
                </GridView>
              </ListView.View>
            </ListView>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,5,0,0">
              <TextBlock Text="{Binding TotalB, StringFormat=Total: {0}}" Foreground="Yellow" VerticalAlignment="Center" FontSize="16"/>
            </StackPanel>
          </StackPanel>
        </StackPanel>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,8,0,0">
          <Button Content="Equalize" Width="100" Height="26" Command="{Binding EqualizeCommand}" IsEnabled="{Binding IsEqualizeEnabled}"/>
          <!--<Button Content="Close" Width="100" Height="26" Margin="10,0,0,0" Command="{Binding CloseCommand}"/>-->
        </StackPanel>
      </StackPanel>
      <!--<StackPanel VerticalAlignment="Center">
      <TextBlock Text="Coming Soon" Foreground="Yellow" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="24">
        <TextBlock.Effect>
          <DropShadowEffect/>
        </TextBlock.Effect>
      </TextBlock>
      <TextBlock Text="Team: See expected score between two teams" TextWrapping="Wrap" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Width="380">
      </TextBlock>
      <TextBlock Text="Player: See expected score between two players" TextWrapping="Wrap" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Width="380">
      </TextBlock>
      <TextBlock Text="Mix: select 4 or 8 players and 'most fair' teams can be generated" TextWrapping="Wrap" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16" Width="380">
      </TextBlock>
    </StackPanel>-->
    </Grid>
    <!--<Border.Effect>
      <DropShadowEffect BlurRadius="15" ShadowDepth="10"/>
    </Border.Effect>-->
    <Border.Background>
      <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
        <GradientStop Color="#FF6C6C6C" Offset="0" />
        <GradientStop Color="#FF868686" Offset="0.3" />
        <GradientStop Color="#FF484848" Offset="1" />
      </LinearGradientBrush>
    </Border.Background>
  </Border>
  <!--<UserControl.Background>
    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
      <GradientStop Color="#FF969696" Offset="0" />
      <GradientStop Color="#FF6C6C6C" Offset="1" />
      <GradientStop Color="#FF484848" Offset="0.684" />
    </LinearGradientBrush>
  </UserControl.Background>-->
</UserControl>
