<UserControl x:Class="Demolyzer.View.ProgressPanelViewControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500" Background="DimGray">
  <Grid>
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
      <Grid>
        <TextBlock Text="Processing..." Foreground="Yellow" FontSize="16" HorizontalAlignment="Left">
          <TextBlock.Effect>
            <DropShadowEffect/>
          </TextBlock.Effect>
        </TextBlock>
      </Grid>
      <ProgressBar Width="400" Height="40" Maximum="{Binding ProgressMax}" Value="{Binding Progress}"/>
      <TextBlock Text="{Binding Description}" Foreground="White" FontSize="16" HorizontalAlignment="Left">
          <TextBlock.Effect>
            <DropShadowEffect/>
          </TextBlock.Effect>
      </TextBlock>
    </StackPanel>
  </Grid>
</UserControl>
